# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Project runtime data (databases, generated feeds, logs)
data/
*.sqlite3
*.sqlite3-journal
calendar.ics
*.log

# Persistent state for the public feed. It retains event identity and is
# committed by GitHub Actions after each successful complete harvest.
!state/
!state/wendeburg.sqlite3
!state/wendeburg.sqlite3-journal

# Local configuration / secrets (root-level only - test fixtures ship their own config.toml)
.env
/config.toml
/config.local.toml

# Test / tooling artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/
